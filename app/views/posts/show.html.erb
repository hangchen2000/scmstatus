<h1><%= @post.title %></h1>

<div><%= @post.body %></div>

<%= link_to "Edit Status", edit_post_path(@post) %>

<%= link_to "Delete Status", post_path(@post), method: :delete, data: {confirm: " are you sure"} %>